apiVersion: v1
kind: Secret
metadata:
  name: secret-clickhouse-cs
  namespace: clickhouse-cs
type: Opaque
data:
  {{- range $key, $val := .Values.secretClickhouseCs.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}