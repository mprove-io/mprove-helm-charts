{{- if .Values.blockml.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.blockml.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.blockml.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}