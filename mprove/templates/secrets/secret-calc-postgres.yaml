{{- if .Values.calcPostgres.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.calcPostgres.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.calcPostgres.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}