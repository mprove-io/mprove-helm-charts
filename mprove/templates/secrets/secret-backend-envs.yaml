{{- if .Values.backend.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.backend.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.backend.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}