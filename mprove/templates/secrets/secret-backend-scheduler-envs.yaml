{{- if .Values.backendScheduler.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.backendScheduler.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.backendScheduler.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}