{{- if .Values.valkey.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.valkey.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.valkey.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}