{{- if .Values.ingressMprove.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mprove
  namespace: {{ ternary "default" "mprove" .Values.ingressMprove.isMaintenance }}
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/tls-acme: "true" 
spec:
  ingressClassName: nginx
  tls: 
  - hosts:
    - {{ .Values.ingressMprove.host }}
    secretName: {{ .Values.gate.tlsSecretName }}
  rules:
    - host: {{ .Values.ingressMprove.host }}
      http:
        paths:
{{- if not .Values.ingressMprove.isMaintenance }}
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 3000
{{- end }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ ternary "maintenance" "front" .Values.ingressMprove.isMaintenance }}
                port:
                  number: 80
{{- end }} 