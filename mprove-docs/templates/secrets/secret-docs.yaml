{{- if .Values.docs.createEnvsSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.docs.envsSecretName }}
type: Opaque
data:
  {{- range $key, $val := .Values.docs.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}
{{- end }}