apiVersion: v1
kind: Secret
metadata:
  name: secret-db-postgres-cluster-view-user
  namespace: db-postgres
type: Opaque
data:
  {{- range $key, $val := .Values.secretDbPostgresViewUser.envs }}
  "{{ $key }}": "{{ $val | toString | b64enc }}"
  {{- end }}